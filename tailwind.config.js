/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx,ts,tsx}"
  ],
  darkMode: "class", // Enable dark mode with 'class' strategy
  theme: {
    extend: {
      // Add any custom colors, fonts, or spacing here later
    },
  },
  plugins: [],
}
